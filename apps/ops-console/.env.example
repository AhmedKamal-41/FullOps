# Copy to .env.local for local development. Never commit real secrets here —
# this is a public SPA client (PKCE, no client secret) talking to services
# that only ever hold fictional demo data (see CLAUDE.md).

VITE_ORDER_SERVICE_URL=http://localhost:8081
VITE_FULFILLMENT_SERVICE_URL=http://localhost:8084
VITE_KEYCLOAK_AUTHORITY=http://localhost:8080/realms/fulfillops
VITE_KEYCLOAK_CLIENT_ID=fulfillops-console
