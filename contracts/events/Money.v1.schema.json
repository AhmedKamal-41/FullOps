{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fulfillops.dev/contracts/events/Money.v1.schema.json",
  "title": "Money.v1",
  "description": "Shared money shape. amount is a decimal string with a fixed scale of 2 (matches java.math.BigDecimal with scale=2, never a float) so amounts round-trip exactly. currencyCode is ISO 4217.",
  "type": "object",
  "additionalProperties": false,
  "required": ["currencyCode", "amount"],
  "properties": {
    "currencyCode": {
      "type": "string",
      "pattern": "^[A-Z]{3}$",
      "description": "ISO 4217 currency code, e.g. \"USD\"."
    },
    "amount": {
      "type": "string",
      "pattern": "^-?[0-9]+\\.[0-9]{2}$",
      "description": "Decimal amount as a string with exactly 2 fractional digits, e.g. \"19.99\"."
    }
  }
}
